# 複製這個檔案為 .env(本機測試用),或直接在部署平台(Render 等)的
# Environment Variables 設定畫面填入同樣的變數名稱與你的 key。
# 千萬不要把真正的 key 寫進這個 .env.example 檔案或 commit 到 GitHub。

OPENAI_API_KEY=你的_OpenAI_API_Key
# 選填,預設是 gpt-4o
OPENAI_MODEL=gpt-4o
